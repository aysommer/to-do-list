import { MenuIcon } from '../../icons/MenuIcon'

import "./TodoMenuButton.css";

const TodoMenuButton = () => {
   return (
      <MenuIcon className="todo-menu-button"/>
   )
}

export default TodoMenuButton;